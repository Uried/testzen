{
  "version": 2,
  "builds": [
    {
      "src": "ionic.config.js",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "www",
        "devCommand": "ionic serve"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
