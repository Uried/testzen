{
  "version": 2,
  "builds": [
    {
      "src": "ionic.config.js",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "www",
        "buildCommand": "npm run build"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
